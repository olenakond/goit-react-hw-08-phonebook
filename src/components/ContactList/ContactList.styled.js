import { styled } from 'styled-components';

export const List = styled('ul')(() => {
  return {
    margin: '30px 0',
    width: 650,
    border: ' 2px outset',
    borderRadius: 10,
    padding: '20px 20px',
  };
});
