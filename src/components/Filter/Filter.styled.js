import { styled } from 'styled-components';

export const Input = styled('input')(() => {
  return {
    marginLeft: 20,
    fontSize: 14,
    color: '#2e2f42',
    width: 300,
    height: 30,
    border: '1px solid #e7e9fc',
    borderRadius: 4,
  };
});
